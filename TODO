PictureDSL - Rewrite plane_points
RectGrid.hs - rewrite squarePoints

Profiling:
  Options to compile with profiling
  -prof -auto-all -rtsopts

Options when running to generate profiling files
  when running +RRS -p -RTS

Look in the haskell guide:
  rts options for heap profiling
